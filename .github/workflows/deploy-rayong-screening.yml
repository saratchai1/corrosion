name: Legacy Rayong GitHub Pages deployment

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - run: echo "GitHub Pages is not enabled for this repository; Rayong is published from the tested branch bundle instead."
