# manifests
`dataset_manifest.json` is the dataset-level manifest. `checksums.sha256` and `raster_validation.json` are populated after raster acquisition/QA; v1 records local raster COGs without committing them.
