# Satellite raster directory

Generated AOI-only COGs are written below by sensor/year. They are ignored by Git in v1 because authenticated Git LFS upload and no-cost quota are not confirmed; checksums and catalog paths still record the local files.
